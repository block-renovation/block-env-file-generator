# NOTE: for ease of use, `get_env.rb` uses an inline bundler install its dependencies at runtime
# If you need to add new dependencies, you should also add them to the `gemfile do` block in `get_env.rb`

gem 'rubocop', require: false
